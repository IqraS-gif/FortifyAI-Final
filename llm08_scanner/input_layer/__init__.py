"""
llm08_scanner.input_layer
=========================
Input Layer: configuration loading, vector DB adapters, and payload library.

Submodules:
    config_loader   — Load and validate YAML config against JSON Schema
    adapters/       — Abstract VectorDBAdapter + concrete implementations
    payload_library — Built-in catalog of attack queries and adversarial vectors

Implementation: Phase 1.
"""
